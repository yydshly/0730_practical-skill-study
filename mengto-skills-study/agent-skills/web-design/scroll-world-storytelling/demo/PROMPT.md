# Scroll World Storytelling — Three-Mode Prompt

## Minimal prompt

Use $scroll-world-storytelling to read my complete story, reduce it to 5–7 beats, choose the strongest of the three modes below, and build a polished responsive scroll-world landing page with one final CTA.

Choose exactly one:

1. **Video scrub** for a generated or filmed cinematic camera move.
2. **Three.js world** for a real-time 3D planet, object, place, or system.
3. **HTML / data / type** for editorial typography, charts, metrics, and semantic DOM.

Before code, return the beat ledger, style bible, chosen mode, and one-sentence rationale. Preserve native reversible scrolling, mobile behavior, reduced motion, and factual integrity.

## Recreate the demo

Use $scroll-world-storytelling to create a portable three-mode reference called The Contract.

- Create a launcher at `demo/index.html`.
- Create separate `video`, `threejs`, and `html-data` demo folders.
- Give every folder an `index.html` and `PROMPT.md`.
- Use local assets, no framework, no build step, and no remote runtime dependency.
- Keep the same six story beats across all modes: Destination, Fence, Bar, Loop, Route, Proof.
- Make each renderer materially different while preserving the same thesis.
- Verify desktop, 390px mobile, reduced motion, local asset loading, reverse scroll, and console state.

## Remix prompt

Use $scroll-world-storytelling with my supplied article. Read it in full, then recommend video, Three.js, or HTML/data as the primary renderer. Explain the choice in one sentence, produce the beat ledger and style bible, write the exact asset and implementation prompts, build the approved mode, and verify the finished page in a real browser.
