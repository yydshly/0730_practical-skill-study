# 验收记录

最后整理日期：2026-08-02（Asia/Shanghai）。本文件区分“既有证据”和“Task 11 最终复验”。`pass` 只表示所列证据范围内通过；`limit` 表示实现已验证，但自动化控制能力或产品范围存在明确边界。浏览器控制层超时既不是产品失败，也不是产品通过。

## 自动化与构建证据

| 日期 | 命令/检查 | 结果 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| 2026-08-02 | `npm.cmd test` | 19 个测试文件，375/375 通过 | pass（Task 10 最终基线） | `.superpowers/sdd/2026-08-01-delphitools-clone-implementation/task-10-report.md`，修复轮次 2 |
| 2026-08-02 | `npm.cmd run build` | TypeScript 与 Vite 成功；344 模块；主包 1757.20 kB，gzip 566.41 kB；有大包警告 | pass with limit（拆包前基线） | 同上 |
| 2026-08-02 | `git diff --check` | 退出码 0；仅 LF→CRLF 提示 | pass（Task 10 基线） | 同上 |
| 2026-08-02 | 图片工具聚焦测试 | 4 文件，86/86 通过；工作区 Canvas/Image 为可控模拟，不当作真实浏览器编码证据 | pass with limit | `task-8-report.md` |
| 2026-08-02 | 高级媒体聚焦测试 | `pdf.test.ts` + `pdf-workspace.test.tsx`，66/66；全量 328/328；构建成功 | pass | `task-9-report.md` 修复轮次 2 |
| 2026-08-02 | 编辑器聚焦测试 | `editor.test.ts` 28/28；全量 375/375；构建成功 | pass with limit（真实图片文件旅程另列历史控制限制） | `task-10-report.md` 修复轮次 2 |
| 2026-08-02 | 精确 56 ID、中文标题/说明/关键词、每个入口动态工作区 | 56 个 ID 精确唯一；无占位工作区；首页不加载 PDF、条码、二维码或编辑器模块 | pass | `tests/registry.test.ts`、`tests/lazy-loading.test.tsx` |
| 2026-08-02 | `npm.cmd test -- --run` | 21 个测试文件，384/384 通过；控制台无异步残留错误 | pass | 最终主控命令输出 |
| 2026-08-02 | `npm.cmd run build` | TypeScript 与 Vite 7.3.6 成功；301 模块；首页主包 168.59 kB，gzip 55.08 kB | pass | 最终主控命令输出与 `dist/assets` |
| 2026-08-02 | 拆包效果 | 首页主包由 1757.20/566.41 kB 降至 168.59/55.08 kB（minified/gzip）；PDF 459.75 kB、开发 1008.50 kB、编辑器 29.85 kB 等按路由加载 | pass | 两次生产构建输出 |
| 2026-08-02 | `npm.cmd audit --json` | 0 个漏洞；此前 5 项开发工具链漏洞已通过 Vite 7.3.6、Vitest 4.1.10 和 React 插件 5.1.4 消除 | pass | npm 官方审计接口最终输出 |
| 2026-08-02 | 静态检查 | `src` 无 `eval`、`new Function`、`fetch`、`XMLHttpRequest`；网址仅为标准命名空间、默认示例或测试安全样本 | pass | 最终 `rg` 检索 |
| 2026-08-02 | `git diff --check` | 最终差异无空白错误 | pass | 最终主控命令输出 |

说明：Task 7 报告记录当时对数学引擎/工作区检索 `eval(` 与 `new Function` 无匹配；这不是最终全仓静态检查的替代品。

## 既有浏览器证据

既有报告没有保存可引用的截图文件路径，因此下表只引用报告中的观察记录，不虚构截图。报告也未记录当时浏览器的完整 URL；这些局部证据不能替代 Task 11 在规范 URL 的最终复验。

| 日期 | URL | 视口 | 主题 | 旅程/观察 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-02 | 未记录 | 1440×1000 | 浅色 | 图片格式转换工作区、格式能力说明、禁用格式中文原因、横向溢出 | pass（局部） | `task-8-report.md`“浏览器证据” |
| 2026-08-02 | 未记录 | 390×844 | 浅色 | 图片转换控件单列、移动导航、横向溢出 | pass（局部） | 同上 |
| 2026-08-02 | 未记录 | 390×844 | 深色 | 正文、工作区、抽屉和控件可辨认，横向溢出 | pass（局部） | 同上 |
| 2026-08-02 | 未记录 | 900×900 | 深色 | 占位图输入 `<本地安全预览>`，生成真实 SVG，显示成功状态和下载入口 | pass（局部） | 同上 |
| 2026-08-02 | 未记录 | 1440×1000（实际内容宽 1425） | 浅色 | Substrata 三栏，添加文字/矩形、调序、移动、撤销、PNG 成功状态；无横向溢出 | pass（无文件旅程） | `task-10-report.md`“浏览器证据” |
| 2026-08-02 | 未记录 | 桌面 | 深色 | 背景/正文可读；Tab 焦点约 2.67px 实线轮廓；无横向溢出 | pass（局部） | 同上 |
| 2026-08-02 | 未记录 | 390×844（实际内容宽 375） | 未记录 | 编辑器单列；图层抽屉打开聚焦关闭按钮，Escape 后抽屉移除且焦点返回触发器 | pass（局部） | 同上 |
| 2026-08-02 | 未记录 | 桌面 | 未记录 | 编辑器点击下载后显示“已开始下载文件”，控制台无错误；未等待浏览器下载事件 | pass with limit | 同上 |

## 两次文件选择控制层超时

| 发生位置 | 动作 | 等待时间 | 已观察事实 | 产品结论 |
| --- | --- | ---: | --- | --- |
| Task 8 图片格式转换 | 文件选择器已打开，控制层调用 `setFiles(tests/fixtures/sample-image.svg)` | 约 466 秒后超时 | 未观察到应用自身报错；控制层没有返回 | **不判失败，也不判通过** |
| Task 10 Substrata | 控制层调用 `setFiles(sample-image.svg)` | 约 776 秒后被中断 | 文件输入动作没有可靠完成，后续真实图片旅程无法建立 | **不判失败，也不判通过** |

只有同时满足以下条件才重测：

1. 只启动一个已确认就绪的验收服务，使用 `http://127.0.0.1:4173/`。
2. fixture 使用浏览器控制层明确可访问的绝对路径；先用最小文件做 `setFiles` 自检。
3. 文件选择调用可设置短超时并在 30 秒内返回；若自检失败，立即停止，不重复等待数百秒。
4. 使用干净浏览器会话，关闭遗留文件选择器，不让旧任务或弹窗占用控制通道。
5. 自检通过后再执行真实 PNG/JPEG/WebP 的“选择 → 浏览器解码 → Canvas 处理 → Blob MIME/文件名 → 预览 → 下载”，以及 Substrata 的“导入两图 → 调序/移动/撤销 → PNG 导出”。

## Task 11 最终真实浏览器复验清单

规范服务：

```powershell
npm.cmd run dev -- --host 127.0.0.1 --port 4173
```

规范 URL：`http://127.0.0.1:4173/`。最终复验使用真实 in-app Chromium、DOM 快照、截图、控制台日志与页面状态；未重复触发已知会长时间卡住的原生 `setFiles` 控制调用。

| URL/视口 | 主题 | 旅程 | 预期证据 | 状态 |
| --- | --- | --- | --- | --- |
| `/`，1440px | 深色→浅色 | 首页、中文“二维码”搜索、分类过滤、固定侧栏、主题切换 | DOM、桌面截图；搜索只保留二维码入口；无横向溢出 | pass |
| `/` 与二维码，1440px | 浅/深 | 主题切换、二维码中文内容生成、SVG 下载状态 | 根主题属性、SVG 尺寸/viewBox、页面“已开始下载文件”状态 | pass |
| 二维码，900×800 | 浅色 | 平板抽屉断点、工具表单、Escape 与焦点返回 | DOM、截图；885/885 内容宽，无横向溢出 | pass |
| `/`、剪贴板图片、编辑器，390×844 | 浅色 | 单列布局、移动抽屉、图片处理与编辑器导出 | 三张页面状态；375/375 内容宽，无横向溢出 | pass |
| 颜色工具 | 任一 | 十个入口、有效/无效输入、图片调色板 | 16 条工作区测试 + 10 条引擎测试；本轮未重复真实文件选择 | limit |
| 文字工具 | 任一 | 文本差异、HTML 安全、DOCX/EPUB/HTML/Markdown 文件结构 | 22 条工作区测试 + 18 条引擎测试；文件结构由自动化验证 | pass with limit（未逐个浏览器下载） |
| 开发工具 | 任一 | QR 生成/下载；条码；正则匹配与恶意回溯 | QR 实际 SVG 与下载状态；`(a+)+$` + 5001 字符在 500ms 后中文超时，页面保持响应 | pass |
| 科学计算器 | 900px | 输入 `2+2`、计算与历史 | 结果 `4`、历史 `2+2 = 4` | pass |
| 基础图片工具 | 390px | 真实 PNG 导入、处理、预览、下载 | 1×1 PNG 经浏览器剪贴板导入；原图/结果 naturalWidth=1；成功与下载状态 | pass |
| 高级媒体 | 任一 | SVG 净化、非 AI 背景移除、追踪、PDF 预检/拼版/Zine | 66 条聚焦测试与真实 PDF/SVG 结构测试；原生文件控制层未重测 | limit |
| `/editor`，390px | 浅色 | 添加文字层、图层抽屉、生成并下载 PNG | Canvas 图层、PNG 成功与下载状态；真实两图 `setFiles` 仍受控制层限制 | pass with limit |
| 正则与 Worker 工具 | 390px | 空闲、处理中、成功、错误、超时终止 | 实际 Worker 恶意回溯超时；控制台无 error/warn | pass |
| `/`、导航抽屉、编辑器抽屉 | 390px | Tab 循环、背景隔离、Escape、焦点返回 | `inert`/`aria-hidden`、首尾焦点循环、焦点返回触发按钮 | pass |
| 代表页面 | 任一 | `prefers-reduced-motion: reduce` | 全局 CSS 明确关闭 transition/animation；浏览器控制层不提供媒体偏好模拟 | limit |

本轮所有浏览器控制台最终均为 0 条 error/warn。下载事件监听对 Blob 下载未返回，但页面自身产生了明确下载状态；因此只把“页面下载动作”判为通过，不把控制层事件捕获写成通过。
